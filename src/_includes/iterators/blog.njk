{% for post in collections.blog %}
<div><a href="{{ post.url }}">{{ post.data.title }}</a></div>
<p>{{ post.data.slug }}<br/>
<span class="fw-lighter">{{ post.date | postDate }}</span></p>
{%- endfor %}
